package not.djinni.presentation.screens.seeker.main.model

import not.djinni.R

enum class VacancyTab(val titleResId: Int) {
    ALL(titleResId = R.string.vacancy_tab_all),
    RECOMMENDED(titleResId = R.string.vacancy_tab_recommended),
}
